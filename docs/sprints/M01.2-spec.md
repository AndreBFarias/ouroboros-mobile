# Sprint M01.2 — Fundação Lib (tokens · motion · haptics · schemas · stores)

```
DEPENDE: M01.1 (commit 11187c1) + micro-fix (commit b679019)
SUCEDE: M01.3 (Componentes UI base parte 1)
ESTIMATIVA: 1-2 horas
```

## Objetivo

Criar a camada de bibliotecas internas que **todos os componentes
seguintes** vão consumir, antes de qualquer componente UI ser escrito.
Sem essa camada bem feita, o resto fica frágil ou inconsistente.

Camadas a entregar (em `src/`):

1. `theme/tokens.ts` — cores Dracula + spacing + radius + typography
2. `lib/motion.ts` — 4 spring presets + 2 timing presets
3. `lib/haptics.ts` — 5 helpers tipados
4. `lib/schemas/pessoa.ts` — `PessoaId`, `PessoaAutor` via zod
5. `config/pessoas.config.ts` — defaults genéricos `Nome_A`/`Nome_B`
6. `config/pessoas.config.example.ts` — template versionado
7. `lib/stores/pessoa.ts` — zustand + persist via SecureStore
8. `lib/stores/persist.ts` — adapter SecureStore para zustand persist
9. Tests unitários em `tests/`

## Sucesso = (todos verificáveis)

1. Arquivos criados nos paths exatos acima.
2. **Tokens** consistentes com `docs/BRIEFING.md` §3 e
   `tailwind.config.js` (mesmos hex codes — ver §1.3 do
   `VALIDATOR_BRIEF.md`).
3. **Motion presets** com damping e stiffness exatos:
   - `subtle: { damping: 22, stiffness: 220 }`
   - `default: { damping: 18, stiffness: 200 }`
   - `bouncy: { damping: 12, stiffness: 180 }`
   - `snappy: { damping: 26, stiffness: 320 }`
4. **Haptics helpers** todos disponíveis: `light`, `medium`,
   `selection`, `success`, `error`. Async, retornam `Promise<void>`.
5. **PessoaId schema** zod aceita exatamente `'pessoa_a' | 'pessoa_b' |
   'ambos'`. **PessoaAutor** aceita `'pessoa_a' | 'pessoa_b'`.
6. **`pessoas.config.ts`** versionado com **defaults genéricos** (nunca
   nomes reais). `pessoas.config.example.ts` idêntico (template
   estável).
7. **Store de pessoa** zustand com:
   - `pessoaAtiva` (PessoaAutor) — quem está usando o app agora
   - `filtroPessoa` (PessoaId) — filtro de visualização
   - `nomes` (Record<PessoaAutor, string>) — runtime, preenchido pelo
     onboarding; default puxa do config.
   - `setPessoaAtiva`, `setFiltroPessoa`, `setNome`
   - Persist via SecureStore (chave `ouroboros.pessoa.v1`)
8. **Tests unitários** (jest preset expo) cobrindo:
   - `schemas/pessoa.ts`: aceita valores válidos, rejeita inválidos
     (incluindo nomes reais como `'andre'`).
   - `lib/motion.ts`: presets têm os números exatos.
   - `config/pessoas.config.ts`: defaults são genéricos (não casam
     `/Andr[eé]|Vit[oó]ria/`).
9. **Verificação:**
   - `npx tsc --noEmit` exit 0
   - `./scripts/smoke.sh` OK
   - `npm test --silent -- --watchAll=false` OK
   - `./scripts/check_anonimato.sh` OK
10. **Commit:** `feat: m01.2 fundacao lib tokens motion haptics schemas stores`

## Restrições

- **Sem nomes reais** em qualquer arquivo. `pessoas.config.ts` usa
  literais `'Nome_A'`, `'Nome_B'`, `'Ambos'`.
- Comentários e mensagens em **PT-BR com acentuação completa**.
- Sem emojis.
- Imports usam alias `@/*` (paths já configurados em `tsconfig.json`).
- Nada de `any` — strict mode ligado.

## Conteúdo Sugerido (referência, não literal)

### `src/theme/tokens.ts`

```ts
// Tokens visuais e de spacing. Fonte da verdade no codigo.
// Espelha tailwind.config.js e docs/BRIEFING.md secao 3.

export const colors = {
  bgPage: '#14151a',
  bg: '#282a36',
  bgAlt: '#1e1f29',
  bgElev: '#44475a',
  fg: '#f8f8f2',
  muted: '#c9c9cc',
  mutedDecor: '#6272a4',
  purple: '#bd93f9',
  pink: '#ff79c6',
  cyan: '#8be9fd',
  green: '#50fa7b',
  yellow: '#f1fa8c',
  orange: '#ffb86c',
  red: '#ff5555',
} as const;

export type ColorToken = keyof typeof colors;

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  base: 16,
  lg: 20,
  xl: 24,
  xxl: 32,
  huge: 48,
  mega: 64,
} as const;

export const radius = {
  input: 10,
  card: 12,
  modal: 16,
  toast: 24,
  chip: 20,
  fab: 28,
  sheet: 20,
} as const;

export const typography = {
  heading1: { size: 24, lineHeight: 1.4, weight: '500' as const },
  heading2: { size: 18, lineHeight: 1.5, weight: '500' as const },
  body: { size: 14, lineHeight: 1.6, weight: '400' as const },
  caption: { size: 12, lineHeight: 1.5, weight: '400' as const },
  micro: { size: 11, lineHeight: 1.4, weight: '400' as const },
} as const;

export const hitSlop = { top: 8, bottom: 8, left: 8, right: 8 } as const;
export const minHitArea = 44;
```

### `src/lib/motion.ts`

```ts
// Spring presets canonicos. Use tipos de moti diretamente.
import type { MotiTransitionProp } from 'moti';

export const springs = {
  subtle: { type: 'spring', damping: 22, stiffness: 220 } as const,
  default: { type: 'spring', damping: 18, stiffness: 200 } as const,
  bouncy: { type: 'spring', damping: 12, stiffness: 180 } as const,
  snappy: { type: 'spring', damping: 26, stiffness: 320 } as const,
} satisfies Record<string, MotiTransitionProp>;

export const timings = {
  fadeOut: { type: 'timing', duration: 180 } as const,
  toastIn: { type: 'spring', damping: 20, stiffness: 250 } as const,
} satisfies Record<string, MotiTransitionProp>;

export type SpringPreset = keyof typeof springs;
```

### `src/lib/haptics.ts`

```ts
// Wrappers tipados sobre expo-haptics. Async para nao bloquear UI thread.
// Uso:
//   await haptics.light();   // botao primario
//   await haptics.success(); // dossie gerado, vitoria registrada
import * as ExpoHaptics from 'expo-haptics';

export const haptics = {
  light: () =>
    ExpoHaptics.impactAsync(ExpoHaptics.ImpactFeedbackStyle.Light),
  medium: () =>
    ExpoHaptics.impactAsync(ExpoHaptics.ImpactFeedbackStyle.Medium),
  selection: () => ExpoHaptics.selectionAsync(),
  success: () =>
    ExpoHaptics.notificationAsync(
      ExpoHaptics.NotificationFeedbackType.Success
    ),
  error: () =>
    ExpoHaptics.notificationAsync(
      ExpoHaptics.NotificationFeedbackType.Error
    ),
} as const;

export type HapticName = keyof typeof haptics;
```

### `src/lib/schemas/pessoa.ts`

```ts
import { z } from 'zod';

// Identidade generica de pessoa. Nunca usar nomes reais (Regra -1).
export const PessoaIdSchema = z.enum(['pessoa_a', 'pessoa_b', 'ambos']);
export type PessoaId = z.infer<typeof PessoaIdSchema>;

export const PessoaAutorSchema = z.enum(['pessoa_a', 'pessoa_b']);
export type PessoaAutor = z.infer<typeof PessoaAutorSchema>;

export const isAutor = (id: PessoaId): id is PessoaAutor =>
  id === 'pessoa_a' || id === 'pessoa_b';
```

### `src/config/pessoas.config.ts` (versionado)

```ts
// UNICO arquivo onde nomes "default" aparecem. Genericos.
// Nomes reais entram em runtime via store (lib/stores/pessoa.ts).
import type { PessoaId } from '@/lib/schemas/pessoa';

export interface PessoaConfig {
  nome: string;
  inicial: string;
  cor: string;
}

export const PESSOAS_CONFIG: Record<PessoaId, PessoaConfig> = {
  pessoa_a: { nome: 'Nome_A', inicial: 'A', cor: '#bd93f9' },
  pessoa_b: { nome: 'Nome_B', inicial: 'B', cor: '#ff79c6' },
  ambos: { nome: 'Ambos', inicial: 'AB', cor: '#bd93f9' },
};

export function inicialDe(pessoa: PessoaId): string {
  return PESSOAS_CONFIG[pessoa].inicial;
}

export function corDe(pessoa: PessoaId): string {
  return PESSOAS_CONFIG[pessoa].cor;
}
```

### `src/config/pessoas.config.example.ts` (versionado, template)

Cópia idêntica de `pessoas.config.ts` (já genérico), serve só como
referência caso alguém regenere o config.

### `src/lib/stores/persist.ts`

```ts
// Adapter SecureStore para zustand persist.
// Storage de runtime sensivel: nomes que o usuario digitou.
import * as SecureStore from 'expo-secure-store';
import type { StateStorage } from 'zustand/middleware';

export const secureStorage: StateStorage = {
  getItem: async (name) => (await SecureStore.getItemAsync(name)) ?? null,
  setItem: async (name, value) => {
    await SecureStore.setItemAsync(name, value);
  },
  removeItem: async (name) => {
    await SecureStore.deleteItemAsync(name);
  },
};
```

### `src/lib/stores/pessoa.ts`

```ts
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import { PESSOAS_CONFIG } from '@/config/pessoas.config';
import type { PessoaAutor, PessoaId } from '@/lib/schemas/pessoa';
import { secureStorage } from '@/lib/stores/persist';

interface PessoaStore {
  pessoaAtiva: PessoaAutor;
  filtroPessoa: PessoaId;
  nomes: Record<PessoaAutor, string>;
  setPessoaAtiva: (p: PessoaAutor) => void;
  setFiltroPessoa: (p: PessoaId) => void;
  setNome: (p: PessoaAutor, nome: string) => void;
}

export const usePessoa = create<PessoaStore>()(
  persist(
    (set) => ({
      pessoaAtiva: 'pessoa_a',
      filtroPessoa: 'pessoa_a',
      nomes: {
        pessoa_a: PESSOAS_CONFIG.pessoa_a.nome,
        pessoa_b: PESSOAS_CONFIG.pessoa_b.nome,
      },
      setPessoaAtiva: (pessoaAtiva) => set({ pessoaAtiva }),
      setFiltroPessoa: (filtroPessoa) => set({ filtroPessoa }),
      setNome: (p, nome) =>
        set((s) => ({ nomes: { ...s.nomes, [p]: nome } })),
    }),
    {
      name: 'ouroboros.pessoa.v1',
      storage: createJSONStorage(() => secureStorage),
    }
  )
);

export function nomeDe(pessoa: PessoaId): string {
  if (pessoa === 'ambos') return PESSOAS_CONFIG.ambos.nome;
  const { nomes } = usePessoa.getState();
  return nomes[pessoa] ?? PESSOAS_CONFIG[pessoa].nome;
}
```

### Tests

`tests/schemas/pessoa.test.ts`, `tests/lib/motion.test.ts`,
`tests/config/pessoas.config.test.ts`. Use jest preset:

`package.json` ganha:
```json
{
  "scripts": {
    "test": "jest --watchAll=false"
  },
  "jest": {
    "preset": "jest-expo",
    "transformIgnorePatterns": ["node_modules/(?!(jest-)?@?react-native|@react-native-community|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|moti|react-clone-referenced-element|@react-native-picker|@react-navigation|nativewind|react-native-css-interop)"]
  }
}
```

Adicionar dev deps: `jest-expo`, `jest`, `@types/jest`.

### Exemplo de teste (`tests/schemas/pessoa.test.ts`)

```ts
import { PessoaIdSchema, PessoaAutorSchema, isAutor } from '@/lib/schemas/pessoa';

describe('PessoaIdSchema', () => {
  it('aceita pessoa_a, pessoa_b e ambos', () => {
    expect(() => PessoaIdSchema.parse('pessoa_a')).not.toThrow();
    expect(() => PessoaIdSchema.parse('pessoa_b')).not.toThrow();
    expect(() => PessoaIdSchema.parse('ambos')).not.toThrow();
  });

  it('rejeita nomes reais', () => {
    expect(() => PessoaIdSchema.parse('andre')).toThrow();
    expect(() => PessoaIdSchema.parse('vitoria')).toThrow();
  });

  it('rejeita string vazia', () => {
    expect(() => PessoaIdSchema.parse('')).toThrow();
  });
});

describe('PessoaAutorSchema', () => {
  it('aceita autor', () => {
    expect(PessoaAutorSchema.parse('pessoa_a')).toBe('pessoa_a');
  });

  it('rejeita ambos como autor', () => {
    expect(() => PessoaAutorSchema.parse('ambos')).toThrow();
  });
});

describe('isAutor', () => {
  it('retorna true para autores', () => {
    expect(isAutor('pessoa_a')).toBe(true);
    expect(isAutor('pessoa_b')).toBe(true);
  });

  it('retorna false para ambos', () => {
    expect(isAutor('ambos')).toBe(false);
  });
});
```

## Critério de Validação

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

./scripts/check_anonimato.sh
./scripts/check_test_data.sh
npx tsc --noEmit
npm test --silent
```

Todos exit 0. Smoke total OK.

## Reporte ao Final

1. Lista de arquivos criados (paths absolutos).
2. Saída literal de `tsc --noEmit`, `npm test`, `./scripts/smoke.sh`.
3. Relatório de cobertura ao menos textual: quantos testes passaram.
4. Achados colaterais (formato `ACHADO: ... | Sugestão: ...`).
5. Hash do commit.
