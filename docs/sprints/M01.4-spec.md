# Sprint M01.4 — Componentes UI Base (parte 2: complexos)

```
DEPENDE: M01.3 (commit 574f4da)
SUCEDE: M01.5 (Checkpoint visual M01)
ESTIMATIVA: 2-3 horas
```

## Objetivo

Implementar **5 componentes complexos** que dependem de gestos,
overlays, física avançada ou multi-elemento. São os blocos que
compõem o "chrome" do app (FAB, BottomTabs) e os primitivos modais
(BottomSheet, Toast, Slider).

## Componentes a Entregar (5)

Em `src/components/ui/`:

1. **`Slider.tsx`** — track 4dp `--bg-elev` com fill `--purple`,
   thumb 24dp purple. Arrastar dispara `haptics.selection()` por step.
   Valor numérico em `--cyan` ao lado (label opcional). Aceita `min`,
   `max`, `step`, `value`, `onChange`. Use
   `@react-native-community/slider` (instalar via `npx expo install`)
   OU implementação manual com PanResponder (escolha a primeira por
   robustez).

2. **`Toast.tsx` + `useToast.tsx` (hook)** — sobe de baixo a 80dp
   do bottom em `springs.default`. Background `--bg-elev` com border-left
   3px na cor semântica (`green` sucesso, `red` erro, `cyan` info, `yellow`
   alerta). Desaparece em 2,5s com fade. Swipe horizontal para dispensar
   antes. Hook `useToast()` retorna `{ show(message, type) }`. Usa
   contexto React global (`<ToastProvider>` em `app/_layout.tsx`).

3. **`BottomSheet.tsx`** — wrapper sobre `@gorhom/bottom-sheet` com
   tema Dracula aplicado (background `--bg-alt`, handle indicator
   `--bg-elev`). Snap points configuráveis. Gesture nativo de fechar
   arrastando para baixo. Backdrop animado (fade até opacity 0,5).
   Aceita `snapPoints`, `index`, `onChange`, `children`.

4. **`FAB.tsx`** — botão 56dp absoluto canto inferior direito.
   Sombra blur 16dp offset `(0, 4)` opacity 30%. Ícone `Plus` da
   `lucide-react-native`. Scale 0.97 + `haptics.medium` ao pressionar.
   Aceita `onPress`, `icon?` (opcional override).

5. **`FABRadial.tsx`** — overlay escuro 50% sobre tela atual. FAB
   rotaciona 45deg (vira X) com `springs.default`. 6 botões circulares
   48dp em arco semicircular subindo do FAB:
   - HUMOR (`pink`)
   - VOZ (`cyan`)
   - CÂMERA (`orange`)
   - EXERCÍCIO (`green`)
   - VITÓRIA (`yellow`)
   - TRIGGER (`red`)
   Cada botão surge em sequência com 60ms delay (`springs.bouncy`).
   Tap fora fecha. Aceita `onSelect: (key: 'humor'|'voz'|'camera'|'exercicio'|'vitoria'|'trigger') => void`.

## Atualizar Sample Screen

`app/_components.tsx` ganha 5 seções novas:

```tsx
<Section title="slider">
  <Slider min={1} max={5} step={1} value={3} onChange={() => {}} />
</Section>
<Section title="toast">
  <Button label="show toast sucesso" onPress={() => toast.show('feito.', 'success')} />
  <Button label="show toast erro" onPress={() => toast.show('falhou.', 'error')} />
</Section>
<Section title="bottom sheet">
  <Button label="abrir sheet" onPress={() => sheetRef.current?.expand()} />
</Section>
<Section title="fab">
  {/* FAB e FABRadial demonstrados em modo overlay */}
</Section>
```

`app/_layout.tsx` recebe **mínima** alteração: envolver `<Stack>` com
`<ToastProvider>` e `<GestureHandlerRootView>`. Esta é a única exceção
ao "não tocar em arquivos fechados" — necessário para BottomSheet e
Toast funcionarem.

## Sucesso = (todos verificáveis)

1. **5 componentes** em arquivo próprio em `src/components/ui/`.
2. **`useToast.tsx`** com hook tipado e Provider exportado.
3. **`src/components/ui/index.ts`** atualizado com novos exports.
4. **`app/_layout.tsx`** envolve Stack com `<GestureHandlerRootView
   style={{ flex: 1 }}>` (necessário para gorhom/bottom-sheet) e
   `<ToastProvider>`.
5. **`app/_components.tsx`** atualizado mostrando os 5 novos.
6. **TypeScript strict** — sem `any`.
7. **Acessibilidade**: todos interativos com `accessibilityRole` e
   `accessibilityLabel` (PT-BR sem acento).
8. **Testes unitários** smoke para cada componente (≥ 5 testes novos):
   - Slider: renderiza, value default, dispatch onChange.
   - Toast: Provider renderiza, hook retorna show.
   - BottomSheet: renderiza closed por default.
   - FAB: render + onPress dispara.
   - FABRadial: 6 botões disponíveis em open state.
9. **Verificação:**
   - `npx tsc --noEmit` exit 0
   - `./scripts/smoke.sh` OK
   - `npm test --silent` OK (≥ 54 testes)
   - `npx expo export --platform android` exit 0
10. **Commit:** `feat: m01.4 componentes ui base parte 2 complexos`

## Restrições

- **Sem nomes reais**, **sem emojis**, **sem `any`**, **sem
  `@ts-ignore`**.
- **Strings de UI** em lowercase com acentuação completa PT-BR
  (`"humor"`, `"voz"`, `"câmera"`, `"vitória"`, `"trigger"`,
  `"feito."`, `"anotado."`).
- **`accessibilityLabel`** sem acento (`"botao humor"`).
- **NÃO mexer** em `src/theme/`, `src/lib/` (M01.2), nem em outros
  componentes da `M01.3`. Pode adicionar exports novos em
  `src/components/ui/index.ts`.
- **`app/_layout.tsx`** alteração mínima: só os providers; não mexer
  na fonte ou no Stack config.
- **`app/index.tsx`** não muda nesta sprint.

## Conteúdo de Referência

### Dependências adicionais

```bash
npx expo install @react-native-community/slider
# @gorhom/bottom-sheet ja foi instalado em M01.1
```

### `src/components/ui/Toast.tsx` (esqueleto)

```tsx
import { createContext, useCallback, useContext, useState, type ReactNode } from 'react';
import { View, Text } from 'react-native';
import { MotiView, AnimatePresence } from 'moti';
import { springs, timings } from '@/lib/motion';
import { colors } from '@/theme/tokens';

type ToastType = 'success' | 'error' | 'info' | 'warn';

interface ToastState {
  message: string;
  type: ToastType;
  id: number;
}

interface ToastApi {
  show: (message: string, type?: ToastType) => void;
}

const ToastContext = createContext<ToastApi | null>(null);

export function ToastProvider({ children }: { children: ReactNode }) {
  const [toast, setToast] = useState<ToastState | null>(null);

  const show = useCallback((message: string, type: ToastType = 'info') => {
    const id = Date.now();
    setToast({ message, type, id });
    setTimeout(() => {
      setToast((cur) => (cur?.id === id ? null : cur));
    }, 2500);
  }, []);

  return (
    <ToastContext.Provider value={{ show }}>
      {children}
      <AnimatePresence>
        {toast && (
          <MotiView
            key={toast.id}
            from={{ translateY: 80, opacity: 0 }}
            animate={{ translateY: 0, opacity: 1 }}
            exit={{ opacity: 0, translateY: 20 }}
            exitTransition={timings.fadeOut}
            transition={timings.toastIn}
            style={{
              position: 'absolute',
              bottom: 80,
              left: 20,
              right: 20,
              backgroundColor: colors.bgElev,
              borderLeftWidth: 3,
              borderLeftColor: borderColorFor(toast.type),
              borderRadius: 24,
              padding: 16,
            }}
            accessibilityRole="alert"
          >
            <Text style={{ color: colors.fg, fontFamily: 'JetBrainsMono_400Regular' }}>
              {toast.message}
            </Text>
          </MotiView>
        )}
      </AnimatePresence>
    </ToastContext.Provider>
  );
}

export function useToast(): ToastApi {
  const ctx = useContext(ToastContext);
  if (!ctx) throw new Error('useToast precisa de ToastProvider acima na arvore');
  return ctx;
}

function borderColorFor(type: ToastType): string {
  switch (type) {
    case 'success': return colors.green;
    case 'error': return colors.red;
    case 'warn': return colors.yellow;
    case 'info': default: return colors.cyan;
  }
}
```

### `src/components/ui/FAB.tsx` (esqueleto)

```tsx
import { Pressable } from 'react-native';
import { MotiView } from 'moti';
import { useState, type ReactNode } from 'react';
import { Plus } from 'lucide-react-native';
import { springs } from '@/lib/motion';
import { haptics } from '@/lib/haptics';
import { colors } from '@/theme/tokens';

interface FABProps {
  onPress: () => void;
  icon?: ReactNode;
}

export function FAB({ onPress, icon }: FABProps) {
  const [pressed, setPressed] = useState(false);

  return (
    <Pressable
      onPressIn={() => { setPressed(true); haptics.medium(); }}
      onPressOut={() => setPressed(false)}
      onPress={onPress}
      accessibilityRole="button"
      accessibilityLabel="acao rapida"
      style={{
        position: 'absolute',
        right: 20,
        bottom: 24,
      }}
    >
      <MotiView
        animate={{ scale: pressed ? 0.95 : 1 }}
        transition={springs.snappy}
        style={{
          width: 56,
          height: 56,
          borderRadius: 28,
          backgroundColor: colors.purple,
          alignItems: 'center',
          justifyContent: 'center',
          shadowColor: '#000',
          shadowOffset: { width: 0, height: 4 },
          shadowOpacity: 0.3,
          shadowRadius: 16,
          elevation: 6,
        }}
      >
        {icon ?? <Plus size={24} color={colors.bg} strokeWidth={2} />}
      </MotiView>
    </Pressable>
  );
}
```

### `src/components/ui/FABRadial.tsx` (esqueleto, simplificado)

Ver `docs/BRIEFING.md` Tela 14 para especificação detalhada. 6 botões em
arco semicircular, 60ms delay entre cada, `springs.bouncy`.

### `app/_layout.tsx` (alteração mínima)

```tsx
import { useFonts, JetBrainsMono_400Regular, JetBrainsMono_500Medium } from '@expo-google-fonts/jetbrains-mono';
import { Stack } from 'expo-router';
import * as SplashScreen from 'expo-splash-screen';
import { useEffect } from 'react';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { ToastProvider } from '@/components/ui/Toast';
import '../global.css';

SplashScreen.preventAutoHideAsync();

export default function RootLayout() {
  const [loaded] = useFonts({
    JetBrainsMono_400Regular,
    JetBrainsMono_500Medium,
  });

  useEffect(() => {
    if (loaded) SplashScreen.hideAsync();
  }, [loaded]);

  if (!loaded) return null;

  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <ToastProvider>
        <Stack
          screenOptions={{
            headerShown: false,
            animation: 'slide_from_right',
            contentStyle: { backgroundColor: '#282a36' },
          }}
        />
      </ToastProvider>
    </GestureHandlerRootView>
  );
}
```

## Critério de Validação

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

./scripts/check_anonimato.sh
npx tsc --noEmit
npm test --silent
./scripts/smoke.sh
npx expo export --platform android --output-dir /tmp/m014-export && rm -rf /tmp/m014-export
```

Todos exit 0. Se `expo export` quebrar com erro de gorhom ou
gesture-handler, registrar como achado e propor fix; **não** mascarar
erro com try/catch ou flag.

## Reporte ao Final

Igual M01.3:

1. Lista de arquivos criados/modificados (paths absolutos).
2. Saída literal de tsc, smoke, npm test, expo export.
3. Quantidade total de testes (esperado ≥ 54).
4. Achados colaterais formatados.
5. **FAÇA O COMMIT** com mensagem `feat: m01.4 componentes ui base parte 2 complexos`
   e reporte o hash. Não deixar working tree suja como na M01.3.

Se algo exigir mexer fora do escopo (ex: nova versão de pacote, mudar
config), reportar REPROVADO formal.
