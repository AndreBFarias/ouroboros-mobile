# Sprint M00.6 — Polish Web Dracula + Snap Presets + Mockup HTML

```
DEPENDE:    M00.5 fechada (tabs, barrels)
BLOQUEIA:   M11.5 (precisa de Tela 25 no mockup HTML)
ESTIMATIVA: 3-4h
```

## 1. Objetivo

Encerrar três dívidas técnicas residuais antes do MVP entrar em
ritmo de captura de telas: tema Dracula desbotado no Web (NativeWind
4 + class), snap points do `<BottomSheet>` em literais espalhados
pelas telas, e o mockup HTML standalone defasado em duas telas
(Tela 25 do calendário de conquistas e widget homescreen). A sprint
sai com Web visualmente fiel à paleta, presets nomeados de snap
points, e mockup atualizado servindo de fonte de verdade visual
para M11.5 e M20.

## 2. Entregáveis

### Arquivos novos

- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/SHEET_PRESETS.ts`
  — constantes nomeadas:
  ```ts
  export const SHEET_60: readonly string[] = ['60%'];
  export const SHEET_70: readonly string[] = ['70%'];
  export const SHEET_80: readonly string[] = ['80%'];
  export const SHEET_90: readonly string[] = ['90%'];
  export const SHEET_DEFAULT: readonly string[] = ['40%', '85%'];
  ```
  Exportar via `src/components/ui/index.ts`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/web/draculaPolish.ts`
  — `applyDraculaWeb()` que injeta CSS adicional via `<style>` no
  `document.head` quando `Platform.OS === 'web'`. Cobre os tokens que
  NativeWind 4 não emite consistentemente:
  - `body { background-color: var(--bg-page); }`
  - `:root { color-scheme: dark; }`
  - Reset de `border-color` para `var(--bg-elev)` em todos elementos
    com classe `border`.
  - Override de `<input>` placeholder para `var(--muted-decor)`.
  - Override de scrollbar (Webkit) para track `--bg-page` e thumb
    `--bg-elev`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/global.css`
  — extender com variáveis CSS na `:root.dark`:
  ```css
  :root.dark {
    --bg-page: #14151a;
    --bg: #282a36;
    --bg-alt: #1e1f29;
    --bg-elev: #44475a;
    --fg: #f8f8f2;
    --muted: #c9c9cc;
    --muted-decor: #6272a4;
    --purple: #bd93f9;
    --pink: #ff79c6;
    --cyan: #8be9fd;
    --green: #50fa7b;
    --yellow: #f1fa8c;
    --orange: #ffb86c;
    --red: #ff5555;
  }
  ```
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/tests/components/ui/SHEET_PRESETS.test.ts`
  — verifica imutabilidade dos arrays e valores.

### Arquivos modificados

- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/_layout.tsx`
  — invocar `applyDraculaWeb()` dentro do mesmo bloco que já força
  `darkMode='class'`. Idempotente (verifica se `<style>` já foi
  injetado).
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/humor-rapido.tsx`
  — `snapPoints={SHEET_70}` em vez de `['70%']`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/diario-emocional.tsx`
  — `snapPoints={SHEET_90}`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/eventos.tsx`
  — `snapPoints={SHEET_80}`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/BottomSheet.tsx`
  — `DEFAULT_SNAP_POINTS` agora vem de `SHEET_DEFAULT`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/index.ts`
  — re-exportar `SHEET_60`, `SHEET_70`, `SHEET_80`, `SHEET_90`,
  `SHEET_DEFAULT`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/docs/Ouroboros_22_telas-standalone.html`
  — adicionar 2 artboards novos:
  - **Tela 25** (Calendário de conquistas) — header laranja
    "Calendário", ChipGroup pessoa, filtros mês/mídia/intensidade/
    bairro, FlatList horizontal de 6 cards 160x200dp com cover de
    foto, YouTube thumb, Spotify, áudio waveform, foto e foto.
    Tap em card abre detalhe.
  - **Tela 26** (Widget homescreen) — preview do widget Android
    pequeno (4x2 dp) e médio (4x4 dp). Mostra inicial da pessoa
    ativa em fundo `--purple`, número grande do humor médio do dia
    em `--cyan`, e atalho FAB radial 4 ícones (humor, voz, câmera,
    vitória). Tap abre app na captura humor.
  - Atualizar índice de telas no topo do HTML para
    "Ouroboros 24 telas-standalone" (mantém nome do arquivo
    standalone para evitar quebrar links existentes; rename só na
    M19).

## 3. APIs reutilizáveis

- `src/theme/tokens.ts` — `colors`, `spacing` para o CSS da Web.
- `src/components/ui/BottomSheet.tsx` — wrapper já existe; só
  recebe presets nomeados.

## 3.5 Integração ao projeto

Conforme `docs/sprints/INTEGRATION-CONTRACT.md`, esta sprint:

- **Atualiza `app/_layout.tsx`:** adiciona invocação
  `applyDraculaWeb()` ao bloco de Dracula force (CONTRACT §1.7).
- **Cria `SHEET_PRESETS.ts` e exporta via `src/components/ui/index.ts`:**
  contrato visual reutilizável.
- **Atualiza HTML standalone:** fonte de verdade visual ganha 2
  artboards novos. M11.5 e M20 dependem.
- **Não toca em tabs, schemas, stores, settings ou app.json.**

## 4. Restrições

- **Regra −1**, sem emojis, Sentence case PT-BR, etc. (padrão).
- **Não trocar** snap points de `['40%', '85%']` (default da
  M01.4) — só renomear como `SHEET_DEFAULT`.
- Mockup HTML editado **a mão dentro do `<style>` e do markup
  inline existente**, sem dependência externa nova.
- Polish web não pode regredir Nível A: validar todas as telas
  M01–M07 antes e depois.

## 5. Procedimento sugerido

1. Criar `src/components/ui/SHEET_PRESETS.ts` com 5 constantes.
   Re-exportar via barrel.
2. Atualizar `BottomSheet.tsx` para usar `SHEET_DEFAULT` como
   fallback.
3. Refatorar `humor-rapido.tsx`, `diario-emocional.tsx`,
   `eventos.tsx` para consumir os presets.
4. Criar `src/lib/web/draculaPolish.ts` com `applyDraculaWeb()`.
5. Estender `global.css` com variáveis CSS da `:root.dark`.
6. Atualizar `app/_layout.tsx` chamando `applyDraculaWeb()` na
   primeira mount.
7. Validar Nível A: paleta correta no Chrome para cada tela
   tocada.
8. Editar `docs/Ouroboros_22_telas-standalone.html` adicionando
   artboards Tela 25 e Tela 26 ao final do bloco de artboards,
   reaproveitando o markup existente como template.
9. Atualizar índice no topo do HTML para refletir 24 artboards
   visuais.
10. Smoke + tests + tsc + expo export.

## 6. Verificação runtime-real

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

./scripts/check_anonimato.sh
npx tsc --noEmit
npm test --silent
./scripts/smoke.sh
npx expo export --platform android --output-dir /tmp/m006-export && rm -rf /tmp/m006-export

# Sanity check: paleta correta no Web
./run.sh --web &
sleep 5
curl -s http://localhost:19006 | grep -q 'background-color' && echo "OK: bg presente"
kill %1
```

Todos exit 0.

## 7. Commit

```
feat: m006 polish web dracula snap presets e mockup html com tela 25 e 26
```

## 8. Checkpoint visual

Política de 3 níveis (`VALIDATOR_BRIEF.md` §1.9):

- **Nível A (Chrome web):** validar paleta Dracula em todas as
  telas existentes (Tela 01, 14, 15, 18, 20, onboarding,
  storybook). Comparar pixel-picker com hex codes esperados.
- **Nível B (emulador):** garantir que app no Android continua
  intacto (apenas Web foi alterado).
- **Nível C (celular):** opcional; nada relacionado ao polish web.

Capturar screenshots em `docs/sprints/M00.6-screenshots/` antes e
depois do polish para diff visual. Capturar Tela 25 e Tela 26
do mockup HTML aberto no Chrome.

## 9. Definição de Pronto

- [ ] `SHEET_PRESETS.ts` exportado via `src/components/ui/index.ts`.
- [ ] Telas existentes consomem os presets nomeados.
- [ ] `applyDraculaWeb()` invocado em `app/_layout.tsx` e funcional
      no Chrome.
- [ ] `global.css` com variáveis CSS da paleta.
- [ ] Mockup HTML standalone tem Tela 25 e Tela 26 renderizadas com
      mesmo estilo dos outros artboards.
- [ ] Smoke completo passa.
- [ ] Screenshots before/after capturadas.

## 10. Decisões tomadas

- **Polish web sem mudar tema:** Dracula continua identidade única
  (ADR-0008). Light mode permanece anti-feature.
- **Snap presets como constantes nomeadas em vez de função:**
  arrays imutáveis simples; type narrowing imediato.
- **Mockup HTML editado a mão:** o arquivo já é gigante (~1.4 MB)
  mas mantém autocontido. Refazer com ferramenta externa quebraria
  a fonte de verdade visual.
- **Nome do arquivo HTML mantido como `Ouroboros_22_telas-standalone.html`:**
  rename para "24 telas" em M19 junto com bump da versão; evitar
  refactors cruzados nesta sprint.

Sprint pronta para execução sem perguntas pendentes.
