# Sprint M11.3 — useLarguraFrame: layouts respeitam frame 412dp em web

```
DEPENDE:    M11.1 fechada
BLOQUEIA:   M-CAPTURA-UNIFICADA (validação visual exige grid correto)
ESTIMATIVA: 1h
PRIORIDADE: alta (vazamento visual confirmado em browser real pelo
            usuário 2026-05-04; bloqueia confiança nos screenshots)
STATUS:     [todo]
```

## 1. Achado / motivação

Validação visual M34 via Gauntlet
(`docs/sprints/M34-screenshots-gauntlet/A-foto-na-galeria.png`)
revelou que ao adicionar **1 foto** via `__gauntlet.adicionarFotoMock()`,
o thumbnail renderiza ocupando **largura quase total** (~ 380px),
não como célula de grid 3-colunas dentro do frame 412dp esperado.

Causa raiz: `MemoriasFotosTab.tsx:46-50` calcula `thumbSize` via
`useWindowDimensions().width` — em web isso retorna a largura do
**viewport** (1280px), não a do frame mobile (412dp aplicado em
`FrameMobileGauntlet`). Resultado: thumbSize ≈ 400dp e cada célula
"vaza" para fora do frame.

```ts
const thumbSize = useMemo(() => {
  const padding = spacing.lg * 2;
  const gaps = spacing.sm * (COLS - 1);
  return Math.floor((dim.width - padding - gaps) / COLS);
}, [dim.width]);
```

Em mobile real, `dim.width` = 412 → thumb ~124dp (correto). Em web
Gauntlet, `dim.width` = 1280 → thumb ~400dp (errado).

## 2. Caminho

Substituir `useWindowDimensions().width` por constante 412dp **em
modo Gauntlet** (web + dev), mantendo dinâmico em release mobile:

```ts
import { Platform } from 'react-native';
import { GAUNTLET_ATIVO } from '@/lib/dev/gauntlet';

const FRAME_W = 412;
const larguraEfetiva = GAUNTLET_ATIVO ? FRAME_W : dim.width;
const thumbSize = Math.floor((larguraEfetiva - padding - gaps) / COLS);
```

Aplicar mesma lógica a outros componentes que usam
`useWindowDimensions` em renders dependentes do frame
(`HeatmapBase`, `Calendar`, etc. — auditar com grep).

## 3. Entregáveis

### Arquivos novos

- `src/lib/ui/useLarguraFrame.ts` — hook centralizador.
  Em web (`Platform.OS === 'web'`) retorna `412` constante; em
  native retorna `useWindowDimensions().width` real.
- `tests/lib/ui/useLarguraFrame.test.ts` — 2 cases (web=412,
  native=passa pelo hook nativo via mock).

### Arquivos modificados (3 consumidores auditados via
`grep useWindowDimensions src/ app/`)

- `src/components/screens/MemoriasFotosTab.tsx:37` — substituir
  `useWindowDimensions().width` por `useLarguraFrame()`.
- `app/medidas/index.tsx:105` — idem.
- `app/exercicios/[slug].tsx:68` — idem.

(Pode haver outros consumidores indiretos via padding/grid; o
executor deve auditar a lista completa antes de tocar e ampliar a
spec se necessário sem dispatchar sub-sprint.)

### Screenshots

- `docs/sprints/M11.3-screenshots-gauntlet/A-grid-fotos-3-cols.png`
  — após `adicionarFotoMock` ×4: grid com 3 colunas + 1 thumb na
  2ª linha + 2 placeholders, todos dentro do frame 412dp.
- `docs/sprints/M11.3-screenshots-gauntlet/B-medidas-no-frame.png`
  — tela `/medidas` com cards comparativos contidos no frame.
- `docs/sprints/M11.3-screenshots-gauntlet/C-exercicio-detalhe-no-frame.png`
  — tela `/exercicios/<slug>` com layout contido.

## 4. Verificação

- `seedComDados('humores-30d')` + `adicionarFotoMock` × 4 → grid
  exibe 3 colunas + 1 célula na 2ª linha + 2 placeholders.
- Largura de cada thumb ~124dp em frame 412.
- `getBoundingClientRect()` do card mais à direita: `right` ≤
  borda direita do frame (846px no viewport 1280px conforme M11.1).
- Em mobile real (Android emulador ou device), comportamento
  inalterado — `useWindowDimensions` continua sendo a fonte.

## 5. Decisões tomadas

- Constante `FRAME_W = 412` é mesma do `FrameMobileGauntlet`. Se um
  dia mudar (M-FRAME-IPAD futuro), centraliza num único token.
- Hook em vez de função pura — consumir via React garante reatividade
  se o frame ganhar variação dinâmica futuramente.
- **Critério `Platform.OS === 'web'` (não `GAUNTLET_ATIVO`)**:
  o frame mobile aplica em qualquer dev web (mesmo fora do
  `__DEV__`), e Hermes em release Android cai pelo branch native
  pois `Platform.OS !== 'web'`.
- **Auditoria preventiva**: `grep useWindowDimensions` em src/ +
  app/ retorna 3 consumidores; todos migram nesta sprint, evitando
  ter que fazer M11.4/M11.5 depois.

## 6. Aritmética esperada

- 1289 → 1291 testes (+2: novos cases do hook).
- 144 → 145 suítes (+1).
- TS strict 0, anonimato OK, smoke OK.
- Bundle Hermes sem regressão (mudança puramente JS).
