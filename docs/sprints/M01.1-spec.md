# Sprint M01.1 — Bootstrap Expo (em-place, preservando docs)

```
DEPENDE: Fase 0 (commit b26c973)
SUCEDE: M01.2 (Fundação Lib)
ESTIMATIVA: 1-2 horas (depende de velocidade do npm)
```

## Objetivo

Inicializar projeto Expo na raiz `~/Desenvolvimento/Protocolo-Mob-Ouroboros/`
**preservando** os arquivos já versionados (`README.md`, `.gitignore`,
`CLAUDE.md`, `CHANGELOG.md`, `LICENSE`, `docs/`, `hooks/`, `scripts/`,
`VALIDATOR_BRIEF.md`).

## Sucesso = (todos os itens verificáveis)

1. `package.json` na raiz com `name: "ouroboros-mobile"` e dependências:
   - **Runtime:** `expo`, `react`, `react-native`, `expo-router`,
     `expo-status-bar`, `expo-splash-screen`, `expo-font`,
     `expo-constants`, `expo-linking`,
     `react-native-safe-area-context`, `react-native-screens`,
     `react-native-gesture-handler`, `react-native-reanimated`
   - **Estilo:** `nativewind@^4`, `tailwindcss@^3`
   - **Animação:** `moti`
   - **UI primitives:** `@gluestack-ui/themed`, `@gluestack-style/react`,
     `@gorhom/bottom-sheet`
   - **Ícones e haptics:** `lucide-react-native`, `expo-haptics`,
     `expo-blur`
   - **Fontes:** `@expo-google-fonts/jetbrains-mono`
   - **Storage:** `expo-file-system`, `expo-secure-store`
   - **Estado e schemas:** `zustand`, `zod`, `yaml`
   - **Dev:** `typescript@^5`, `@types/react`, `eslint`,
     `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin`,
     `prettier`
2. `app/_layout.tsx` carrega JetBrains Mono (400 e 500), background
   `#282a36`, animation `slide_from_right`, headerShown false.
3. `app/index.tsx` tela placeholder em background Dracula com texto
   mono lowercase "ouroboros-mobile · m01.1 ok".
4. `tailwind.config.js` com **paleta Dracula completa** (`bg`, `bg-alt`,
   `bg-elev`, `fg`, `muted`, `muted-decor`, `purple`, `pink`, `cyan`,
   `green`, `yellow`, `orange`, `red`) e fontFamily mono.
5. `babel.config.js` com presets `babel-preset-expo` (jsxImportSource
   nativewind) e plugin `react-native-reanimated/plugin` **por último**
   (Armadilha A1).
6. `metro.config.js` com `withNativeWind`.
7. `tsconfig.json` strict + paths `@/*` apontando para `src/*` (e
   também `app/*` se útil).
8. `app.json` com:
   - `name: "ouroboros-mobile"`
   - `slug: "ouroboros-mobile"`
   - `orientation: "portrait"`
   - `userInterfaceStyle: "dark"` (Dracula é único tema, ADR-008)
   - `android.package: "com.ouroboros.mobile"`
   - plugins: `expo-router`, `expo-font`, `expo-secure-store`
9. `nativewind-env.d.ts` na raiz para tipagem de className.
10. `global.css` com `@tailwind` directives para o NativeWind 4.
11. **Verificação:**
    - `npx tsc --noEmit` → exit 0
    - `npx expo-doctor` → sem erros críticos
    - `./scripts/smoke.sh` → OK
    - `git status` → working tree limpo após commit
12. **Commit:** `feat: m01.1 bootstrap expo com paleta dracula e jetbrains mono`

## Restrições (NÃO quebrar)

- **NÃO sobrescrever** os arquivos existentes:
  - `README.md`, `CLAUDE.md`, `CHANGELOG.md`, `LICENSE`, `.gitignore`,
    `VALIDATOR_BRIEF.md`
  - `docs/`, `hooks/`, `scripts/`
- **NÃO criar** `node_modules/` na raiz antes do `.gitignore` (já existe
  e ignora).
- **Preservar** o histórico git: trabalhar em commits incrementais.
- **Sem** introduzir nomes reais no código gerado.
- **Sem** referências a IA em comentários.
- **Sem** emojis em qualquer arquivo gerado.

## Procedimento Recomendado

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

# 1. Gerar template Expo numa pasta temporária para não colidir
npx create-expo-app@latest /tmp/expo-bootstrap --template blank-typescript --no-install

# 2. Copiar SÓ os arquivos que faltam, preservando os nossos
rsync -a --ignore-existing /tmp/expo-bootstrap/ .

# 3. Sobrescrever app.json, tsconfig.json com nossos valores customizados
#    (ver Sucesso #8 e #7)

# 4. Limpar template indesejado
rm -rf /tmp/expo-bootstrap

# 5. Adicionar dependências em uma única passada
#    (NativeWind 4 + Reanimated — ver Armadilha A7)
npm install \
  nativewind@latest tailwindcss@^3 \
  moti react-native-reanimated react-native-gesture-handler \
  @gluestack-ui/themed @gluestack-style/react \
  @gorhom/bottom-sheet \
  lucide-react-native expo-haptics expo-blur \
  @expo-google-fonts/jetbrains-mono expo-font \
  expo-router expo-secure-store expo-file-system expo-linking \
  expo-splash-screen expo-status-bar expo-constants \
  react-native-safe-area-context react-native-screens \
  zustand zod yaml

npm install --save-dev typescript@^5 @types/react eslint \
  @typescript-eslint/parser @typescript-eslint/eslint-plugin prettier

# 6. Inicializar Tailwind
npx tailwindcss init

# 7. Escrever os arquivos de config (tailwind.config.js, babel.config.js,
#    metro.config.js, tsconfig.json, app.json, global.css, app/_layout.tsx,
#    app/index.tsx, nativewind-env.d.ts) conforme conteúdos abaixo.
```

## Conteúdos de Referência

### `tailwind.config.js`

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./app/**/*.{js,jsx,ts,tsx}", "./src/**/*.{js,jsx,ts,tsx}"],
  presets: [require("nativewind/preset")],
  theme: {
    extend: {
      colors: {
        'bg-page': '#14151a',
        bg: '#282a36',
        'bg-alt': '#1e1f29',
        'bg-elev': '#44475a',
        fg: '#f8f8f2',
        muted: '#c9c9cc',
        'muted-decor': '#6272a4',
        purple: '#bd93f9',
        pink: '#ff79c6',
        cyan: '#8be9fd',
        green: '#50fa7b',
        yellow: '#f1fa8c',
        orange: '#ffb86c',
        red: '#ff5555',
      },
      fontFamily: {
        mono: ['JetBrainsMono_400Regular'],
        'mono-medium': ['JetBrainsMono_500Medium'],
      },
    },
  },
  plugins: [],
};
```

### `babel.config.js`

```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      ['babel-preset-expo', { jsxImportSource: 'nativewind' }],
      'nativewind/babel',
    ],
    // reanimated/plugin DEVE ser o ultimo (Armadilha A1)
    plugins: ['react-native-reanimated/plugin'],
  };
};
```

### `metro.config.js`

```js
const { getDefaultConfig } = require('expo/metro-config');
const { withNativeWind } = require('nativewind/metro');

const config = getDefaultConfig(__dirname);

module.exports = withNativeWind(config, { input: './global.css' });
```

### `global.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### `nativewind-env.d.ts`

```ts
/// <reference types="nativewind/types" />
```

### `app/_layout.tsx`

```tsx
import { useFonts, JetBrainsMono_400Regular, JetBrainsMono_500Medium } from '@expo-google-fonts/jetbrains-mono';
import { Stack } from 'expo-router';
import * as SplashScreen from 'expo-splash-screen';
import { useEffect } from 'react';
import '../global.css';

SplashScreen.preventAutoHideAsync();

export default function RootLayout() {
  const [loaded] = useFonts({
    JetBrainsMono_400Regular,
    JetBrainsMono_500Medium,
  });

  useEffect(() => {
    if (loaded) SplashScreen.hideAsync();
  }, [loaded]);

  if (!loaded) return null;

  return (
    <Stack
      screenOptions={{
        headerShown: false,
        animation: 'slide_from_right',
        contentStyle: { backgroundColor: '#282a36' },
      }}
    />
  );
}
```

### `app/index.tsx`

```tsx
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View className="flex-1 bg-bg items-center justify-center px-5">
      <Text className="text-fg font-mono text-base">
        ouroboros-mobile
      </Text>
      <Text className="text-muted-decor font-mono text-xs mt-2">
        m01.1 ok
      </Text>
    </View>
  );
}
```

### `tsconfig.json` (essencial)

```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}
```

### `app.json` (essencial)

```json
{
  "expo": {
    "name": "ouroboros-mobile",
    "slug": "ouroboros-mobile",
    "version": "0.1.0",
    "orientation": "portrait",
    "userInterfaceStyle": "dark",
    "scheme": "ouroboros",
    "icon": "./assets/icon.png",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "cover",
      "backgroundColor": "#282a36"
    },
    "android": {
      "package": "com.ouroboros.mobile",
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#282a36"
      }
    },
    "plugins": [
      "expo-router",
      "expo-font",
      "expo-secure-store"
    ],
    "experiments": {
      "typedRoutes": true
    }
  }
}
```

> Os arquivos `assets/*.png` (icon, splash, adaptive-icon) virão do
> template — se não existirem, deixar referência mas não fazer falhar o
> bundle. Em última instância, criar PNGs vazios placeholder
> (`512x512` transparente) para os assets faltantes.

## Critério de Validação

Após implementar, **executar e capturar saída** dos comandos abaixo:

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

./scripts/check_anonimato.sh        # esperado: OK
./scripts/check_test_data.sh        # esperado: OK
npx tsc --noEmit                    # esperado: exit 0
npx expo-doctor                     # esperado: poucos avisos, zero erros
ls -la                              # confirmar: README, CLAUDE, etc preservados
cat package.json | jq .name         # esperado: "ouroboros-mobile"
git status --short                  # esperado: vazio após commit
git log --oneline -3                # esperado: commit m01.1 + commit bootstrap
```

E reportar tudo no comentário final, sem omitir warnings.

## Achados Colaterais

Se descobrir algo durante execução que não está no spec mas é crítico
(ex: dependência faltante que quebra build, conflito de versão real),
**reportar no comentário final** com:

- O achado
- Por que afeta a sprint
- Sugestão de correção (sem aplicar — eu valido e abro M01.1.1 se
  necessário)
