# Sprint M01.6 — Refinamento Estético (Sentence case, acentos, respiração)

```
DEPENDE: M01.5 (commit be971f4)
SUCEDE: M02 (Vault Bridge + Tela 01)
ESTIMATIVA: 1-2 horas
```

## Objetivo

Aplicar as 4 ressalvas levantadas no checkpoint visual M01.5
(`docs/sprints/M01.5-checkpoint-visual.md`):

- **R1.** Migrar todas as strings de UI para **Sentence case** com
  acentuação completa PT-BR (revoga regra "lowercase intencional" do
  BRIEFING).
- **R2.** Adicionar acentos faltantes em palavras como `botoes`,
  `padrao`, `anotacao`, `diario`, `irritacao`, `cansaco`,
  `notificacoes`, `o que voce sentiu hoje.`, etc.
- **R3.** Aumentar respiração visual: gap entre seções 24 → 40,
  espaço acima do título da seção 16 → 24, gap interno entre items
  8 → 12-16, line-height body 1,5 → 1,6.
- **R4.** Suprimir warning de `SafeAreaView` deprecado (vem de
  dependência transitiva) via `LogBox.ignoreLogs([...])` em
  `app/_layout.tsx`.

## Sucesso = (todos verificáveis)

1. **R1+R2** — varredura final por
   `grep -rE "(?<!\w)(botoes|padrao|anotacao|diario|irritacao|cansaco|notificacoes|voce|nao|acoes|registrar)" src/ app/ --include='*.tsx' --include='*.ts'`
   retorna apenas hits em **comentários** ou
   **`accessibilityLabel`**, nunca em string visível.
2. **R1** — strings de UI em Sentence case. Mapeamento esperado:

   | Antes (lowercase) | Depois (Sentence case) |
   |---|---|
   | `"botoes"` | `"Botões"` |
   | `"primary action"` | `"Ação primária"` |
   | `"success"` | `"Sucesso"` |
   | `"ghost"` | `"Fantasma"` (ou manter `"Ghost"`) |
   | `"destructive"` | `"Destrutivo"` |
   | `"disabled"` | `"Desabilitado"` |
   | `"cards"` | `"Cards"` |
   | `"card padrao em repouso."` | `"Card padrão em repouso."` |
   | `"borda purple. scale 0.99 ao pressionar."` | `"Borda purple. Scale 0,99 ao pressionar."` |
   | `"inputs"` | `"Inputs"` |
   | `"anotacao"` | `"Anotação"` |
   | `"diario"` | `"Diário"` |
   | `"escreva aqui."` | `"Escreva aqui."` |
   | `"o que voce sentiu hoje."` | `"O que você sentiu hoje."` |
   | `"chips single"` | `"Chips single-select"` |
   | `"chips multi"` | `"Chips multi-select"` |
   | `"serenidade"` | `"Serenidade"` |
   | `"ansiedade"` | `"Ansiedade"` |
   | `"irritacao"` | `"Irritação"` |
   | `"cansaco"` | `"Cansaço"` |
   | `"casa"` / `"trabalho"` / `"lazer"` | `"Casa"` / `"Trabalho"` / `"Lazer"` |
   | `"chip avulso"` | `"Chip avulso"` |
   | `"toggle"` | `"Toggle"` |
   | `"modo silencioso"` | `"Modo silencioso"` |
   | `"notificacoes"` | `"Notificações"` |
   | `"avatares"` | `"Avatares"` |
   | `"empty state"` | `"Empty state"` |
   | `"nada anotado por enquanto."` | `"Nada anotado por enquanto."` |
   | `"slider"` | `"Slider"` |
   | `"intensidade"` | `"Intensidade"` |
   | `"toast"` | `"Toast"` |
   | `"toast sucesso"` | `"Toast sucesso"` |
   | `"toast erro"` | `"Toast erro"` |
   | `"toast info"` | `"Toast info"` |
   | `"bottom sheet"` | `"Bottom sheet"` |
   | `"abrir sheet"` | `"Abrir sheet"` |
   | `"fab e fab radial"` | `"FAB e FAB radial"` |
   | `"abrir radial"` | `"Abrir radial"` |
   | `"ultima acao"` | `"Última ação"` |
   | `"feito."` | `"Feito."` |
   | `"falhou."` | `"Falhou."` |
   | `"anotado."` | `"Anotado."` |
   | `"storybook m01"` | `"Storybook M01"` |
   | Toast types/labels do FABRadial: `"humor"`, `"voz"`, `"camera"`, `"exercicio"`, `"vitoria"`, `"trigger"` | `"Humor"`, `"Voz"`, `"Câmera"`, `"Exercício"`, `"Vitória"`, `"Trigger"` |

   Lista **não exaustiva**; aplicar a regra a todas as strings
   visíveis.

3. **R3** — espaçamento atualizado:
   - `Section` no `app/_components.tsx`: gap entre seções de 24 →
     **40**.
   - Espaço acima do título da seção: 16 → **24**.
   - Gap interno entre items dentro de seção: 8 → **12-16**.
   - Padding top da Screen: incluir `pt-6` (ou `paddingTop: 24`).
   - Line-height body em `src/theme/tokens.ts` (typography.body):
     1.6 → **1.6** (já está; verificar se está sendo aplicado).
   - Verificar que `<Text>` em componentes como Card description,
     Toast e Input placeholder usa `lineHeight: 22-24` (ou
     equivalente classe Tailwind).

4. **R4** — `app/_layout.tsx` ganha:

   ```tsx
   import { LogBox } from 'react-native';

   LogBox.ignoreLogs([
     'SafeAreaView has been deprecated',
   ]);
   ```

5. **Acessibilidade não regride**: continua tendo
   `accessibilityRole` + `accessibilityLabel` em todos os
   interativos. **`accessibilityLabel` continua sem acento**
   (convenção PT-BR de screen readers).

6. **Testes não quebram**:
   - Atualizar testes que dependem de string específica para refletir
     o novo casing (ex: `getByText("primary action")` vira
     `getByText("Ação primária")`).
   - Total continua ≥ 65, todos passando.

7. **Verificação:**
   - `npx tsc --noEmit` exit 0
   - `./scripts/smoke.sh` OK
   - `npm test --silent` OK
   - `npx expo export --platform android --output-dir /tmp/m016-export`
     exit 0

8. **Commit:** `style: m01.6 sentence case acentuacao e respiracao visual`

## Restrições

- **Sem nomes reais**, **sem emojis**, **sem `any`**.
- **NÃO mexer** em `src/theme/tokens.ts` exceto para confirmar
  line-height; **NÃO mexer** em `src/lib/`, `src/config/`,
  `src/lib/stores/`. Único arquivo "fechado" autorizado de tocar
  desta vez: `app/_layout.tsx` (R4) e qualquer arquivo de componente
  em `src/components/ui/` (para corrigir strings) e em `app/_components.tsx`
  (storybook + spacing).
- Imports via alias `@/*`.

## Procedimento Recomendado

1. Procurar todas strings de UI em `app/_components.tsx` e
   `src/components/ui/*.tsx`. Aplicar Sentence case + acentos.
2. Atualizar `Section` interno de `_components.tsx` para gap maior.
3. Suprimir warning em `app/_layout.tsx`.
4. Rodar testes; corrigir asserts que dependem de string antiga.
5. Validar `npm test --silent` 100% passando.
6. Validar `npx expo export` exit 0.
7. Commit.

## Critério de Validação

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros
./scripts/check_anonimato.sh
npx tsc --noEmit
npm test --silent
./scripts/smoke.sh
npx expo export --platform android --output-dir /tmp/m016-export && rm -rf /tmp/m016-export
```

Todos exit 0.

## Reporte ao Final

Igual sprints anteriores. Inclua tabela `antes -> depois` das
strings que mudou (pelo menos as 10 mais visíveis), saída literal de
comandos, hash do commit. **Faça o commit** com mensagem exata
acima.
