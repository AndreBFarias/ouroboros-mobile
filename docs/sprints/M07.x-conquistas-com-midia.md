# Sprint M07.x — Conquistas com Mídia Obrigatória

```
DEPENDE:    M06 fechada (Tela 18 com modo vitoria salvando em
              inbox/mente/diario/)
            + M07 fechada (Tela 20 com modo positivo salvando em
              eventos/)
            + M06.5 fechada (gravação de áudio, pré-requisito para
              tipo 'audio' do MidiaPicker)
BLOQUEIA:   M11.5 (Calendário visual de conquistas — depende deste
              schema com mídia para renderizar covers)
ESTIMATIVA: 5-7h
```

> **DEPENDÊNCIA CRÍTICA — TIPO `audio`.** O tipo de mídia `audio` do
> componente `<MidiaPicker>` reutiliza o pipeline de gravação criado
> na M06.5 (`src/lib/diario/recordAudio.ts`). Esta sprint **assume**
> que M06.5 está fechada. Se M06.5 ainda não estiver disponível,
> ofertar versão reduzida do `<MidiaPicker>` com 3 abas (Spotify /
> YouTube / Foto) e adicionar a aba `Áudio` em sprint M07.x.1
> posterior. Confirmar com humano antes de executar.

> **DEPENDÊNCIA CRÍTICA — DECISÃO ARQUITETURAL SPOTIFY.** Spotify
> Web API exige PKCE flow + token em SecureStore (rede + auth
> contraria ADR-0007 zero telemetria). Esta spec adota a alternativa
> simples: aceitar **link colado**, extrair `track_id` via regex,
> fazer scrape do oEmbed `https://open.spotify.com/oembed?url=...`
> uma única vez para metadata, **sem auth**. Se humano preferir
> Web API completa, abrir sprint INFRA-spotify-auth antes desta.

## 1. Objetivo

Tornar **mídia obrigatória** ao registrar conquistas. Quando o usuário
marca `modo: 'vitoria'` em `diario_emocional` (Tela 18) ou `modo:
'positivo'` em `evento` (Tela 20), o save **só** acontece se houver
pelo menos um item em um array `midia: []`. Tipos suportados: Spotify
(link), YouTube (link), Foto (galeria/câmera) e Áudio (gravação
on-device). Para `modo: 'trigger'` / `modo: 'negativo'`, mídia
permanece **opcional** — pedir mídia em momento de crise adiciona
fricção indevida (ADR-0005, sem gamificação).

A ideia: conquistas são marcos que merecem peso emocional duradouro;
forçar a captura de mídia evita registros vazios e dá material rico
para o calendário/timeline (M11.5).

## 2. Entregáveis

### Arquivos novos

- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/schemas/midia.ts`
  — schema zod canônico de mídia:
  ```ts
  export const MidiaSpotifySchema = z.object({
    tipo: z.literal('spotify'),
    track_id: z.string().min(1),
    titulo: z.string().optional(),
    artista: z.string().optional(),
    url_oembed: z.string().url().optional(),
  });
  export const MidiaYoutubeSchema = z.object({
    tipo: z.literal('youtube'),
    video_id: z.string().min(1),
    titulo: z.string().optional(),
    thumbnail_url: z.string().url().optional(),
  });
  export const MidiaFotoSchema = z.object({
    tipo: z.literal('foto'),
    path: z.string().min(1),
  });
  export const MidiaAudioSchema = z.object({
    tipo: z.literal('audio'),
    path: z.string().min(1),
    duracao_seg: z.number().positive().optional(),
  });
  export const MidiaSchema = z.discriminatedUnion('tipo', [
    MidiaSpotifySchema, MidiaYoutubeSchema, MidiaFotoSchema, MidiaAudioSchema,
  ]);
  export type Midia = z.infer<typeof MidiaSchema>;
  ```
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/MidiaPicker.tsx`
  — componente principal com 4 abas (Spotify / YouTube / Foto /
  Áudio). Controle:
  - prop `value: Midia[]`, `onChange: (m: Midia[]) => void`.
  - prop `obrigatorio: boolean` (mostra micro caption red abaixo
    se `obrigatorio && value.length === 0`).
  - cada aba renderiza sua sub-tela (componente próprio).
  - lista preview dos itens já adicionados em grid 2 colunas com
    botão `X` para remover.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/MidiaSpotifyTab.tsx`
  — input "Cole o link da música" + botão `"Adicionar"`. Extrai
  `track_id` via regex
  (`/spotify\.com\/track\/([a-zA-Z0-9]+)/`). Faz fetch único do
  oEmbed (`https://open.spotify.com/oembed?url=<link>`) para pegar
  título e artista (campos `title` e `author_name`). Erros
  silenciosos: link inválido = micro caption red `"Link de Spotify
  inválido."`. Se oEmbed falhar (sem rede), salva só `track_id`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/MidiaYoutubeTab.tsx`
  — input "Cole o link do vídeo" + botão `"Adicionar"`. Extrai
  `video_id` via regex que aceita `youtube.com/watch?v=`,
  `youtu.be/` e `youtube.com/shorts/`. Thumbnail derivada
  diretamente: `https://img.youtube.com/vi/<id>/hqdefault.jpg`
  (sem chamada de rede). Se link inválido, micro caption red
  `"Link de YouTube inválido."`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/MidiaFotoTab.tsx`
  — botão `"Escolher da galeria"` (`expo-image-picker
  launchImageLibraryAsync`) e botão `"Tirar foto"`
  (`launchCameraAsync`). Foto retornada é copiada para
  `assets/<formatDateYmdHm>-conquista-<idx>.jpg` antes de adicionar
  ao array.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/MidiaAudioTab.tsx`
  — reusa `<MicrofoneButton>` da M06.5 com modo "captura simples"
  (sem transcrição). Botão `"Gravar áudio"` press-to-hold; salva em
  `assets/<formatDateYmdHm>-conquista-<idx>.m4a` e adiciona com
  `duracao_seg`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/midia/WaveformPreview.tsx`
  — preview estático de áudio gravado: ícone Mic + duração em
  segundos + barra de play (`<Audio.Sound>` para reproduzir).
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/midia/spotifyOEmbed.ts`
  — `fetchSpotifyOEmbed(url): Promise<{ title?: string; author_name?:
  string }>`. Fetch único, sem auth. Timeout 5s. Erro = retorna
  objeto vazio.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/midia/youtubeId.ts`
  — `extractYouTubeId(url): string | null` com regex completa.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/midia/spotifyId.ts`
  — `extractSpotifyTrackId(url): string | null` com regex completa.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/tests/lib/schemas/midia.test.ts`
  — cobre `discriminatedUnion`: cada `tipo` aceita seus campos e
  rejeita estranhos.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/tests/lib/midia/youtubeId.test.ts`
  — extração de ID a partir dos 3 formatos (`watch?v=`, `youtu.be/`,
  `shorts/`).
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/tests/lib/midia/spotifyId.test.ts`
  — extração de track_id de URL canônica.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/tests/components/midia/MidiaPicker.test.tsx`
  — render das 4 abas, troca de aba, micro caption red quando
  `obrigatorio && value.length === 0`, adicionar e remover item.

### Arquivos modificados

- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/schemas/diario_emocional.ts`
  — adicionar campo `midia: z.array(MidiaSchema).default([])` no
  `.object` e refinar com `.refine` que **bloqueia** save se
  `modo === 'vitoria' && midia.length === 0`. Mensagem de erro do
  refine: `"vitoria exige pelo menos uma midia"`. Atualizar `type
  DiarioEmocionalMeta`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/schemas/evento.ts`
  — adicionar `midia: z.array(MidiaSchema).default([])` e `.refine`
  que bloqueia se `modo === 'positivo' && midia.length === 0`.
  Atualizar `type EventoMeta`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/diario-emocional.tsx`
  — adicionar `<MidiaPicker obrigatorio={modo === 'vitoria'}
  value={midia} onChange={setMidia} />` no form. Passar
  `midia` para `meta` do save.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/app/eventos.tsx`
  — adicionar `<MidiaPicker obrigatorio={modo === 'positivo'} />` e
  passar para `meta`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/CHANGELOG.md`
  — entrada `[Unreleased]` "Sprint M07.x - midia obrigatoria em
  conquistas (vitoria/positivo) com MidiaPicker quatro tipos.".

## 3. APIs reutilizáveis

- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/Chip.tsx`
  — chips para abas Spotify / YouTube / Foto / Áudio.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/Input.tsx`
  — inputs de URL.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/Button.tsx`
  — botões `"Adicionar"`, `"Escolher da galeria"`, `"Tirar foto"`,
  `"Gravar áudio"`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/ui/Toast.tsx`
  — toasts: `"Mídia adicionada."`,
  `"Adicione pelo menos uma mídia para conquista."` (red).
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/components/diario/MicrofoneButton.tsx`
  — reutilizado em `MidiaAudioTab` em modo "captura simples".
  Confirmar prop opcional `transcribe?: boolean` (default `true`)
  ou expor 2 modos.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/diario/recordAudio.ts`
  — `startRecording`, `stopRecording`, `saveRecordingToVault`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/vault/paths.ts`
  — `assetsPath` e (já criado em M06.5) `assetsAudioPath`.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/motion.ts`
  — `springs.subtle` para troca de aba.
- `/home/andrefarias/Desenvolvimento/Protocolo-Mob-Ouroboros/src/lib/haptics.ts`
  — `haptics.light()` no botão Adicionar.
- `expo-image-picker` — galeria e câmera.
- `expo-av` — `Audio.Sound` para preview do áudio.

## 4. Restrições

- **Regra −1** (Anonimato): zero referência a IA, zero nomes reais.
- Sem emojis em código, docs ou commits.
- Strings de UI em **Sentence case com acentuação completa PT-BR**
  (ADR-0013). Ex: `"Mídia"`, `"Spotify"`, `"YouTube"`,
  `"Foto"`, `"Áudio"`, `"Cole o link da música"`,
  `"Cole o link do vídeo"`, `"Escolher da galeria"`,
  `"Tirar foto"`, `"Gravar áudio"`, `"Adicionar"`,
  `"Adicione pelo menos uma mídia para conquista."`,
  `"Link de Spotify inválido."`, `"Link de YouTube inválido."`,
  `"Mídia adicionada."`.
- `accessibilityLabel` sem acento (ex: `'aba spotify'`,
  `'remover midia'`).
- Comentários em código sem acento.
- Mensagens de commit sem acento.
- TypeScript strict.
- Imports via alias `@/*`.
- **Não tocar** em `src/components/ui/*` (M01).
- **Spotify**: aceitar apenas link colado. **Sem auth**. **Sem PKCE**.
  Sem token em SecureStore. oEmbed scrape único, sem rede recorrente.
- **YouTube**: thumbnail derivada do ID, sem chamada de rede.
  Reprodução do vídeo está **fora de escopo** desta sprint
  (somente cover estático).
- **Áudio**: reusa pipeline da M06.5. Sem fallback de nuvem.
- **Modo trigger / negativo**: mídia continua opcional. UI mostra
  o `<MidiaPicker obrigatorio={false}>` mas não bloqueia o save.
- **Limite por registro**: 4 mídias máximo (cap interno; UI
  desabilita ao atingir).
- **Migração de schema**: o `.refine` adicionado em
  `diario_emocional` e `evento` é **breaking** para arquivos `.md`
  antigos sem `midia`. O default `.default([])` no campo cuida do
  parsing, mas o refine só dispara em **novos saves**. Arquivos
  existentes sem `midia` continuam legíveis (são reverenciados como
  sem mídia, sem mensagem de erro).

## 5. Procedimento sugerido

1. Criar `src/lib/schemas/midia.ts` com `discriminatedUnion`.
2. Criar 3 helpers:
   - `src/lib/midia/youtubeId.ts`
   - `src/lib/midia/spotifyId.ts`
   - `src/lib/midia/spotifyOEmbed.ts`
3. Atualizar schemas:
   - `src/lib/schemas/diario_emocional.ts`: adicionar `midia` +
     `.refine` (vitoria exige pelo menos uma).
   - `src/lib/schemas/evento.ts`: idem para positivo.
4. Criar componentes em `src/components/midia/`:
   - `WaveformPreview.tsx`
   - `MidiaSpotifyTab.tsx`
   - `MidiaYoutubeTab.tsx`
   - `MidiaFotoTab.tsx`
   - `MidiaAudioTab.tsx`
   - `MidiaPicker.tsx` (orquestrador)
5. Cabear em `app/diario-emocional.tsx`:
   - novo estado `midia: Midia[]`.
   - inserir `<MidiaPicker obrigatorio={modo === 'vitoria'}
     value={midia} onChange={setMidia} />` antes do botão final.
   - travar save se `obrigatorio && midia.length === 0`.
   - passar `midia` para `meta`.
6. Cabear em `app/eventos.tsx`:
   - mesmo padrão com `obrigatorio={modo === 'positivo'}`.
7. Testes (4 arquivos novos).
8. Comandos da seção 6.
9. Commit.

## 6. Verificação runtime-real

```bash
cd ~/Desenvolvimento/Protocolo-Mob-Ouroboros

./scripts/check_anonimato.sh
npx tsc --noEmit
npm test --silent
./scripts/smoke.sh
npx expo export --platform android --output-dir /tmp/m07x-export && rm -rf /tmp/m07x-export
```

Todos exit 0. Total de testes deve aumentar (esperado: +10 a +14).

Validação manual após save em modo vitoria com 1 link de Spotify
+ 1 foto: rodar
`cat ~/Protocolo-Ouroboros/inbox/mente/diario/$(date +%F)*.md`,
confirmar frontmatter contém:
```yaml
midia:
  - tipo: spotify
    track_id: <id>
    titulo: "<...>"
  - tipo: foto
    path: ./assets/<...>.jpg
```

## 7. Commit

```
feat: m07x conquistas com midia obrigatoria quatro tipos
```

## 8. Checkpoint visual

Esta sprint **toca UI** (componente `<MidiaPicker>` novo, integrado
em duas telas). Política de 3 níveis (`VALIDATOR_BRIEF.md` §1.9):

- **Default — Nível A (Chrome web):** `./run.sh --web` +
  claude-in-chrome MCP. Capturar 7 screenshots:
  1. Tela 18 modo vitoria sem mídia (mostra micro caption red e
     botão Salvar desabilitado).
  2. `<MidiaPicker>` aba Spotify aberta com link colado.
  3. `<MidiaPicker>` aba YouTube com thumbnail renderizada.
  4. `<MidiaPicker>` aba Foto com 2 fotos adicionadas em grid.
  5. `<MidiaPicker>` aba Áudio com `<WaveformPreview>` mostrando
     duração.
  6. Tela 20 modo positivo com 3 mídias misturadas (1 Spotify, 1
     YouTube, 1 foto).
  7. Toast `"Mídia adicionada."` após cada add.
- **APIs nativas — Nível B (emulador Android):**
  `./scripts/start-emulator.sh`. Validar que galeria, câmera e
  gravação de áudio retornam URIs reais. Capturar mais 3
  screenshots cobrindo image picker do emulador, câmera fake e
  gravação real.
- **Final — Nível C (celular físico):** **só com permissão
  explícita do usuário**. Validar fluxo real ponta-a-ponta com
  mídia mista; verificar no desktop após sync que `assets/`
  contém os arquivos copiados e o frontmatter referencia paths
  relativos corretos.

Salvar capturas em `docs/sprints/M07.x-screenshots/`. Como esta
sprint não corresponde a uma única tela do BRIEFING, o checkpoint
visual mostra o componente em ação dentro das Telas 18 e 20.

## 9. Dúvidas em aberto

- **Spotify auth**: spec adota oEmbed scrape sem auth para
  simplicidade (zero rede recorrente, sem token, sem PKCE).
  Confirmar com humano se essa simplificação é aceitável; caso
  contrário, abrir sprint INFRA-spotify-auth antes desta.
- **Cap de 4 mídias**: número arbitrário; revisar após uso real.
- **Reprodução de vídeo YouTube**: sprint atual mostra somente
  thumbnail + link. Tap no preview abre `Linking.openURL(url)` no
  navegador externo (decisão default; alternativa = embed
  `react-native-youtube-iframe`). Confirmar com humano se quer
  embed inline ou link externo.
- **Backward compatibility**: arquivos `.md` antigos sem `midia`
  continuam parseáveis (`.default([])`), mas perdem o refinement
  de vitoria/positivo (não há como bloquear retroativamente).
  Decisão atual: refine só vale para novos saves; arquivos
  legados ficam intocados.
- **Decisão arquitetural pendente**: marcar M07.x como blocante
  da M11.5 (Calendário visual). Spec atual marca como `BLOQUEIA:
  M11.5` no cabeçalho — confirmar com humano que esse acoplamento
  é desejado, ou se M11.5 deve apenas **renderizar** mídia quando
  ela existir (sem exigir M07.x para começar).
