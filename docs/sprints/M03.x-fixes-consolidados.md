# Família M03.x — Fixes Consolidados

```
PERIODO:    2026-04-29
SPRINTS:    M03.1, M03.2, M03.3, M03.4, M03.5, M03.6, M03.7
COMMITS:    85bf549 -> 71951ea -> e0358a9 -> de7d1dd -> 63c4ec0 -> ce68bac -> b23274a
TOTAL:      ~5h cumulativos de fix pós checkpoint visual
```

Este documento consolida a cascata de fixes aplicados após o
checkpoint visual da Sprint M03 (Onboarding) no Redmi Note 13 5G
Pro. Serve como referência histórica para entender por que aquela
sequência de mudanças aconteceu, sem precisar do histórico da
conversa original.

## Contexto

A Sprint M03 entregou o Onboarding em 4 frames (commit `35aaa1d`),
mas o checkpoint visual no celular real revelou múltiplos problemas
de polish e UX. O usuário relatou-os de forma incremental ao longo
de testes manuais, e cada relato virou uma sub-sprint de fix
imediata.

## Quadro de fixes

| Sprint | Commit | Problema relatado | Fix aplicado |
|---|---|---|---|
| M03.1 | `85bf549` | App piscando entre `/` e `/onboarding` no boot. Labels micro-laranja em lowercase sem acento ("antes de comecar"). Cards do Frame 3 muito juntos. | Hook `useHasHydrated()` em `src/lib/stores/hydrated.ts` aguarda zustand-persist hidratar antes de qualquer `<Redirect>`. Labels viraram Sentence case com acentuação ("Antes de começar", "Companhia", "Vault", "Sincronização"). Removido `textTransform: lowercase`. Gap entre cards do Frame 3: 24dp → 40dp. |
| M03.2 | `71951ea` | Faltava foto de perfil no onboarding. | `<AvatarPicker pessoa={...}>` em `src/components/ui/AvatarPicker.tsx` abre galeria via `expo-image-picker`, copia foto para `documentDirectory/avatars/<pessoa>.jpg` e persiste em `usePessoa.fotos`. `<PersonAvatar>` ganha prop opcional `photoUri`. Frame 0 mostra picker acima do input de nome; Frame 1 mostra picker da segunda pessoa. `app.json` ganha plugin `expo-image-picker` com permissions PT-BR. |
| M03.3 | `e0358a9` | Validar onboarding sem desinstalar app a cada teste. | Botão "Resetar onboarding" em primeira seção do storybook (`app/_components.tsx`). Limpa `useOnboarding`, `usePessoa`, `useVault.clearVaultRoot()`, redireciona para `/`. Action `usePessoa.resetar()` adicionada. |
| M03.4 | `de7d1dd` | Chip selecionado "Casal" ilegível (texto purple sobre fundo purple). FAB radial com labels truncados ("Con...", "Câm..."). Frames com tela em branco entre transições. Avatar do header da Tela 01 sem foto. Tags do humor em `underscore_case`. | Chip: removeu `Moti.animate({backgroundColor})` (bug com transparent → hex sem `from`); `bg/border` viraram `style` direto, texto não-selecionado mudou para `colors.fg`. FAB radial: container `<View width: 160 alignItems: flex-end>` para label, pill ajusta ao texto. Onboarding: removeu `<AnimatePresence>` + `exit`; só spring de entrada via `key={frame}`. Tela 01: PersonAvatar do header agora recebe `photoUri={fotoAtiva}`. Helper `formatTag()` em `app/index.tsx` converte `trabalho_pesado` → `Trabalho pesado`. |
| M03.5 | `63c4ec0` | Trocar foto não atualizava (cache do `<Image>` por URI). API `MediaTypeOptions` deprecada. | Path da foto vira `<pessoa>-<timestamp>.jpg` (timestamp invalida cache). Apaga foto antiga via `FileSystem.deleteAsync` antes de salvar nova. Trocou `MediaTypeOptions.Images` por `mediaTypes: ['images']` (API SDK 54). |
| M03.6 | `ce68bac` | Avatar do header da Tela 01 ainda mostrava toggle A/B mesmo quando usuário escolheu "Sozinho" no onboarding. | `app/index.tsx` lê `useOnboarding.tipoCompanhia`. Se sozinho, `onAvatarPress = undefined` (avatar fica fixo, sem clique). Type de `ConteudoProps.onAvatarPress` muda para `(() => void) \| undefined`. |
| M03.7 | `b23274a` | Labels superiores do FAB radial (Vitória, Trigger, Exercício) sobrepondo os ícones — porque ângulos próximos (252°, 270°) faziam labels ficarem na mesma altura. | Distinção lateral vs superior: itens com `angleDeg >= 240` recebem label **acima** do círculo (centralizado), em vez de à esquerda. Ângulos voltaram para range matemático canônico 180-270° + ARC_RADIUS aumentou de 175 para 210dp. |

## Decisões arquiteturais derivadas dos fixes

Três decisões importantes saíram desta cascata e foram formalizadas
em ADRs durante a Sprint M00.docs:

1. **ADR-0013 (Capitalização revogada)**: a M03.1 forçou a revisão
   do "lowercase intencional" originalmente prescrito no
   `docs/BRIEFING.md` §1 e §2.4. Sentence case com acentuação PT-BR
   completa virou regra.

2. **ADR-0015 (Identidade dinâmica com foto)**: a M03.2 estendeu a
   ADR-0011. Nomes em `usePessoa` com `setNome()`, fotos via
   AvatarPicker em `usePessoa.fotos` com `setFoto()`, ambos
   persistidos em SecureStore.

3. **Política de validação 3 níveis** (`VALIDATOR_BRIEF.md` §1.9):
   formalizada após percebemos que validar tudo no celular físico
   gerava conflito com uso real do dispositivo. Nível A (Chrome
   web), Nível B (emulador), Nível C (celular com permissão).

## Aprendizados operacionais

- **Cascata de fixes pós-checkpoint é normal** quando uma sprint de
  UI grande (M03 entregou 5 frames) chega ao celular real. Plano
  preventivo: cada checkpoint visual gera **lista priorizada de
  fixes**, e cada fix vira sub-sprint pequena (1-2h) com commit
  isolado. Histórico fica auditável.

- **Hidratação de zustand-persist é assíncrona**. Qualquer gate de
  redirect baseado em estado persistido precisa esperar
  `hasHydrated()`. Hoje há um hook helper em
  `src/lib/stores/hydrated.ts`.

- **`<Image>` do RN cacheia por URI**. Para invalidar cache, mudar
  o path (não só o conteúdo). Timestamp no nome é suficiente.

- **NativeWind 4 + Reanimated 4 + Web** tem fricção com:
  - `import.meta` em pacotes ESM (zustand devtools middleware) →
    fix via `unstable_conditionNames` no `metro.config.js`.
  - `expo-secure-store` sem implementação web → fix via adapter
    `localStorage` em `src/lib/stores/persist.ts`.
  - `darkMode: 'media'` default não aplica paleta no Chrome → fix
    parcial via `darkMode: 'class'` + `documentElement.classList.add('dark')`.

## Estado final pós-M03

Tag git: nenhuma específica para M03. A próxima tag será
`v0.2.0-m00-docs` ao fim da Sprint M00.docs.

Métricas em `STATE.md` no commit `ce80b12`:

- 110/110 testes passando
- 24 suítes
- Bundle Android: 7,58 MB Hermes
- expo-doctor: 17/17 checks
- Onboarding em 4 frames + foto de perfil + identidade dinâmica
  validados no Redmi Note 13 5G Pro

## Ver também

- `docs/sprints/M01.5-checkpoint-visual.md` — checkpoint que iniciou
  a cascata.
- `docs/sprints/M03-spec.md` — sprint mãe.
- `docs/ADRs/0013-regra-capitalizacao-revogada.md`
- `docs/ADRs/0015-pessoas-runtime-foto.md`
- `VALIDATOR_BRIEF.md` §1.9 (política de validação 3 níveis)
