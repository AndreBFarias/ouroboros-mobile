# M07.x — Adendo do Planejador

Validacoes pre-execucao do spec `M07.x-conquistas-com-midia.md`. Le
este arquivo ANTES de comecar a sprint. Adendo nao substitui spec; complementa.

## 1. MicrofoneButton — prop `transcribe?` NAO existe ainda

`src/components/diario/MicrofoneButton.tsx` exporta interface
`MicrofoneButtonProps` com **dois callbacks obrigatorios**:
`onTextoTranscrito` e `onAudioGravado`. Nao ha prop `transcribe?`
nem modo "captura simples".

**Decisao para esta sprint** (escolher na ordem):
- Opcao A (preferida, low-risk): em `MidiaAudioTab`, passar
  `onTextoTranscrito={() => {}}` (no-op) e usar so o
  `onAudioGravado` para alimentar `MidiaAudioSchema`. Sem refactor
  do MicrofoneButton.
- Opcao B: refatorar para `transcribe?: boolean` (default `true`)
  e tornar `onTextoTranscrito?` opcional. So fazer se A travar
  na pratica. Refactor = sprint propria, nao escopo M07.x.

Comecar pela A. Se o transcribe rodar inutilmente em background
sem custo perceptivel, manter A. Se travar UX, dispatchar
INFRA-microfone-modo-simples para o planejador.

## 2. Acentuacao periferica — alerta forte

Lisao INFRA-acentuacao (commit `a792156`) reabriu deficit em 145
arquivos. Para EVITAR repetir:

- Comentarios em `.ts` / `.tsx` SEMPRE sem acento (regra ja no
  BRIEF §1.4) — escrever desde a primeira linha, nao corrigir depois.
- Strings de UI SEMPRE com acento completo PT-BR (regra ja no
  spec §4) — `"Mídia"`, `"Áudio"`, `"Adicione pelo menos uma mídia
  para conquista."`.
- `accessibilityLabel` SEM acento (`'aba spotify'`, `'remover midia'`).
- Antes do commit, varredura proativa nos 13 arquivos novos +
  4 modificados:
  ```bash
  for f in $(git diff --name-only HEAD); do
    grep -nP '[áéíóúâêôãõàç]' "$f" | grep -v "^[0-9]*:[^/]*//" || true
  done
  ```
  Linhas que casarem ESTAO em comentario (errado) ou em string de
  UI (correto). Inspecionar manualmente.

## 3. Aritmetica de testes confirmada

- Baseline real: 955 passing / 112 suites (medido agora).
- Esperado pos-M07.x: 965-969 passing / 116 suites (+10 a +14).
- Arquivos novos de teste: 4
  - `tests/lib/schemas/midia.test.ts` (~4 cases discriminatedUnion)
  - `tests/lib/midia/youtubeId.test.ts` (~3 formatos URL)
  - `tests/lib/midia/spotifyId.test.ts` (~2 cases)
  - `tests/components/midia/MidiaPicker.test.tsx` (~4 cenarios)
- FAIL_BEFORE = 0; FAIL_AFTER deve continuar 0.

## 4. Registry-check NAO necessario

`expo-image-picker@~17.0.11`, `expo-av@~16.0.8` e
`expo-file-system@~19.0.22` ja em `package.json`. Sem novas deps =
sem registry-check (regra dispara so com `npm install <novo>`).

## 5. Schema `evento.ts` — campo `audio` ausente

Diferente de `diario_emocional.ts`, `evento.ts` NAO tem `audio`
hoje. Spec adiciona so `midia`. Coerente — eventos nao tem
gravacao de audio fora do contexto de midia. Confirmar que o
`.refine` para `modo === 'positivo'` nao quebre nenhum teste
existente em `tests/lib/schemas/evento*.test.ts`.

## 6. Touches autorizados — fronteira

Spec lista 13 arquivos novos + 4 modificados. NAO tocar em:
- `src/components/ui/*` (M01, intocavel)
- `src/lib/diario/recordAudio.ts` (M06.5, intocavel)
- `src/components/diario/MicrofoneButton.tsx` (intocavel se Opcao A em §1)
- `src/lib/stores/settings.ts` (chaves `midia.*` ja existem; so consumir)

Se durante execucao surgir necessidade real de tocar fora desse
escopo, PARAR e dispatchar nova sprint INFRA-* via planejador.
